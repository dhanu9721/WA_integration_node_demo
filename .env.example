# Server
PORT=3000

# AiSensy credentials
AISENSY_PROJECT_ID=your_project_id_here
AISENSY_API_PWD=d63fc3e62131c04d53a5c
AISENSY_TO=9742569189

# CORS — the frontend origin(s) allowed to call this backend.
# Comma-separated. Use * to allow all (fine for demo, not for production).
ALLOWED_ORIGINS=*
